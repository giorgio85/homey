<h2 class="text-2xl font-bold text-center mb-8">Forgot Password?</h2>

<%= form_with(model: resource, as: resource_name, url: user_password_path, method: :post, class: "space-y-4") do |f| %>
  <div>
    <%= f.label :email, class: "block text-sm font-medium text-gray-700" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-4 py-2 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
  </div>

  <div>
    <%= f.submit "Send me reset password instructions", class: "w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 focus:outline-none" %>
  </div>
<% end %>

<div class="text-center mt-4">
  <%= link_to "Remembered your password? Log in", new_user_session_path, class: "text-sm text-blue-500 hover:underline" %>
</div>